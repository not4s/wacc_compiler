sample_programs/invalid/syntaxErr/function/functionReturnInLoop.wacc
calling the reference compiler on sample_programs/invalid/syntaxErr/function/functionReturnInLoop.wacc
-- Test: functionReturnInLoop.wacc

-- Uploaded file: 
---------------------------------------------------------------
# function body missing a guaranteed return (the loop might not be entered)

# Output:
# #syntax_error#

# Exit:
# 100

# Program:

begin
  int f(int n) is
    int i = 0;
    while i < n do
      i = i + 1;
      if i >= n 
      then 
        return i 
      else
        skip
      fi
    done
  end
  int x = call f(10) ;
  println x 
end
---------------------------------------------------------------

-- Compiler Output:
-- Compiling...
Errors detected during compilation! Exit code 100 returned.
Syntactic Error at 12:2 -- Function f is not ended with a return or an exit statement.
1 parser error(s) detected, no further compilation attempted.
