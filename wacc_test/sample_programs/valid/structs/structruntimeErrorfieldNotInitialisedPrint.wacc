# when trying to use an uninitialised element of a struct in an expression (e.g. printing) this should throw an
# uninitialised value error.

# Output:
# TODO: figure out how format of runtime error message
# Output:

# Program:

begin
  struct MyStruct begin
    int elem;
  end
  struct MyStruct _my_abstract_var_name_ ;
  println _my_abstract_var_name_.elem
end